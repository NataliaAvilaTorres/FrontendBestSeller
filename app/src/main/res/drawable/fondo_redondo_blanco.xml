<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Fondo blanco -->
    <solid android:color="@android:color/white" />

    <!-- Sombra/borde gris claro -->
    <stroke
        android:width="1dp"
        android:color="#DDDDDD" />

    <!-- Elevación visual (solo en versiones nuevas se nota) -->
    <size
        android:width="40dp"
        android:height="40dp" />
</shape>